# Experimental Outputs — 2026-09-09

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 21:45 — first briefing-chart pack filled `/tmp` (ENOSPC)

**Source:** user paste
**Run:** `/tmp/pi_briefing_charts_2026-09-09` pack from the PI-briefing fetch list

```
tar: Removing leading `/' from member names
tar: Removing leading `/' from hard link targets
...
rsync: [receiver] write failed on "/tmp/pi_briefing_charts_2026-09-09/wan_cite128/rolling_notta_h30s_shard0/pixel_full/fvd_i3d/panda_0054.npz": No space left on device (28)
...
gzip: stdout: No space left on device
-rw-r--r--. 1 wc3013 wc3013 0 Sep  9 21:45 /tmp/pi_briefing_charts_2026-09-09.tgz
```

**Interpretation:** `/tmp` is the wrong landing zone, and the first pack
copied the wrong files. The killer was `pixel_full/fvd_i3d/*.npz` plus
every per-clip sidecar `*.json`. Charts only need population summaries,
`vbench_full/joined.json`, and a few compact CSVs. Redo on `/scratch`,
extract in place, never copy `.npz` / `.mp4` / per-clip sidecars.

---

## 21:49 — `/tmp` still 98% after pack delete; scratch is fine

**Source:** user paste / torch-login-a-1
**Run:** `rm -rf /tmp/pi_briefing_charts_2026-09-09*` then `df -h`

```
Filesystem                                          Size  Used Avail Use% Mounted on
tmpfs                                               2.0G  2.0G   45M  98% /tmp
vast-eth.torch.hpc.nyu.edu:/vast/hpc/torch/scratch  3.3P  2.8P  585T  83% /scratch
```

**Interpretation:** Deleting our pack freed almost nothing. Something else
owns the 2 GB tmpfs. Do not pack into `/tmp`. Set `TMPDIR` to scratch
before Python so gzip/tempfile cannot ENOSPC the 45M leftover. Scratch
has 585T free — that is the only landing zone.

---

## 21:52 — pack script missing on cluster after pull to 07b6234

**Source:** user paste / torch-login-a-1
**Run:** `TMPDIR=/scratch/wc3013/tmp`; `git pull`; `python3 scripts/pack_pi_briefing_charts.py`

```
3.5M	/tmp/metacam_result_lsjson_heartbeat8.json
...
119M	/tmp/pf-inspect
...
Updating f5b960e..07b6234
python3: can't open file '.../scripts/pack_pi_briefing_charts.py': [Errno 2]
```

**Interpretation:** `/tmp` is other users (wig_hills, pf-inspect, …). Leave
it. `TMPDIR` on scratch is correct. Pull stopped at `07b6234`; the pack
script was only on the laptop. Push the script, then pull again.

